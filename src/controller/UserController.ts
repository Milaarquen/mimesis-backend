import { Request, Response } from "express";

export default class UserController {
    async signup(
        req:Request,
        res:Response
    ){
        try {
            
        } catch (error) {
            
        }
    }
}